{
  "name": "geo-diagnostics",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phase 1 GEO diagnostic fetchers (robots per-bot rules, UA cloaking diff, rendering-gap, schema presence) run against a mock site.",
  "scripts": {
    "demo": "tsx src/runDiagnostics.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "demo:proxy": "tsx src/runProxyDemo.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.127.0",
    "cheerio": "^1.0.0",
    "playwright": "^1.48.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
